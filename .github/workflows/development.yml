name: Development workflow example

on:
  workflow_call:
  workflow_dispatch:

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: Do something
        run: |
          echo "yay! we can do something clever here"
